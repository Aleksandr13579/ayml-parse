@Library('yaml') _

pipeline {
    agent any

    options {
        timestamps()
    }

    parameters {
        string(name: 'ARCHIVE_1', description: 'the name of the first archive')
        string(name: 'ARCHIVE_2', description: 'the name of the second archive')
    }

    stages{
        stage('second'){
            steps {
                script {

                    HelloWorld(this)
                }
            }
        }
    }
}