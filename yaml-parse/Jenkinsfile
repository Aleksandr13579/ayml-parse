@Library('yaml')

pipeline {
    agent any

    stages{
        stage('second'){
            steps {
                script {
                    sh 'pwd && ls -alrt'
                    sh 'ls -alrt ./yaml-parse'
                    sh 'ls -alrt ./yaml-parse/vars'

                }
            }
        }
    }
}