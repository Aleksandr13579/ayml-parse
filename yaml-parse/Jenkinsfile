@Library('yaml') _

pipeline {
    agent any

    stages{
        stage('second'){
            steps {
                script {
                    HelloWorld(this)
                }
            }
        }
    }
}