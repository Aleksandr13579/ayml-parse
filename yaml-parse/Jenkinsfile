@Library('yaml') _

pipeline {
    agent any

    stages{
        stage('second'){
            steps {
                script {
                    sh 'pwd && ls -alrt'
                    sh 'ls -alrt ./yaml-parse'
                    sh 'ls -alrt ./yaml-parse/vars'
                    HelloWorld()
                }
            }
        }
    }
}